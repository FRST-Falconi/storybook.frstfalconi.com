import styled from 'styled-components'

interface AvatarI {
    size?: string,
    src?: any,
    disabled?: boolean,
}

export const WrapperCard = styled.div`
    width: 244px;
    min-height: 31px;
    display: flex;
    justify-content: center;
    align-items: center;

    
`
