import React from 'react'
import OnboardingProgress from './index'


export default {
    title: 'DS/OnboardingProgress',
    component: OnboardingProgress,
}

const Template = (args) => <OnboardingProgress {...args} />

export const normal = Template.bind({})
normal.args = {
    
}